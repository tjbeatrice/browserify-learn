{
  "name": "browserify-first",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/main.js -o dist/bundle.js",
    "minify": "browserify src/main.js | uglifyjs > dist/bundle.js",
    "watch": "watchify src/main.js -o dist/bundle.js --debug --verbose",
    "serve": "python -m SimpleHTTPServer 8000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "uglify-js": "^2.7.0",
    "uniq": "^1.0.1",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "backbone": "^1.3.3",
    "inherits": "^2.0.1",
    "jquery": "^3.1.0"
  }
}
